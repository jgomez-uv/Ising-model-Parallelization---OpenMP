The given code is the implementation of an ising model developed with the metropolis algorithm for stabilization under physical constraints of minimization of energy, the conditions utilized are of a 2D lattice of spinned particles with bounding term of J = 1 and external magnetic field B = 0. The code in both cases (sequential and parallelized) divides itself into two parts, first it studies the thermalization process of a lone bigger lattice, giving an output of initial and final snapshots of the lattice, and an evolution of magnetization and energy of the system. The second part of the codes studies the correlation length of different lattices under different temperatures, showing critical temperature behavior of this length peaking around the expected critical temperature.

The two sets of code given are the two versions of the same implementation, one being specifically designed to be of sequential, minimal execution, the second a next iteration of the first aiming for parallelization wherever possible with OpenMP tools. The parallelized processes are the calculation of energy and magnetization of a given grid, the correlation lenght extraction of a given temperature in different independent runs for the sake of averaging, and the correlation calculation of a single given grid (onto which later the correlation length is extracted from), this last one being disabled in the code given its process is already a part of a parallelized bigger process.

Now, as an explanation and apology given the very late submission of this work on the context of this being a work submission for a parallelization class. I am terribly sorry for completely missing the given deadline, I was unable to make a functioning sequential code that could comply with the expectations of the second part of the task, any and all implementations worked on in and out of class hours would fail to show the expected behaviors, only after completely scrapping two times this process at the third try I was able, with the help of AI in terms of guidance (not immediately copy-pasted code, at least), to get a functioning code that could then be parallelized. I only write this to give the context I consider necessary given my lack of communication around the subject. I do not expect of this explanaition to be sufficient to avoid mayor penalizations of the given grade, and I only expect your judgement to be what you consider just, I tried to show work being done in class, but it is finally your decision to believe I actually tried or not, and if that justifies at least partially this blatantly overdue submission.
